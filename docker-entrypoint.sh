#!/bin/bash

# 容器启动脚本 - 保持容器运行
# 所有安装和启动操作由宿主机脚本通过 docker exec 执行

echo "============================================"
echo "  云崽容器已启动，等待宿主机指令..."
echo "  可通过 docker exec $CONTAINER_NAME ... 操作"
echo "============================================"

# 保持容器运行
tail -f /dev/null